import { Component, Input } from '@angular/core';

import { TEAMS } = from './teams';

@Component({
  selector: 'team-generator',
  template: `
  <div>
    it works team generator
  </div>`,
})
export class TeamGeneratorComponent  {
  
}
